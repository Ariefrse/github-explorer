# GitHub API Configuration
VITE_GITHUB_API_BASE_URL=https://api.github.com
VITE_GITHUB_API_VERSION=2022-11-28
VITE_GITHUB_TOKEN=your_github_personal_access_token_here

# App Configuration
VITE_APP_NAME=DevHub Explorer
VITE_APP_VERSION=1.0.0
VITE_PER_PAGE=20

# Instructions:
# 1. Copy this file to .env
# 2. To get higher rate limits, create a GitHub Personal Access Token:
#    - Go to GitHub Settings > Developer settings > Personal access tokens > Tokens (classic)
#    - Generate a new token with 'public_repo' scope
#    - Add the token to VITE_GITHUB_TOKEN
# 3. The app will work without a token but with lower rate limits